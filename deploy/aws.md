# Deploy to AWS (ECS / EKS / Lambda)
# 1. Build image: docker build -t customer-ai-agent .
# 2. Push to ECR
# 3. Deploy to ECS Fargate or EKS
# 4. Configure ALB, RDS Postgres, ElastiCache Redis
# 5. Set secrets in AWS Secrets Manager
